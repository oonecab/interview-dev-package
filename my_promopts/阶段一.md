你现在在 `/Users/chunwang/Code/interview-dev-package` 工作。

请先阅读以下文件，理解项目要求，但本阶段不要一次性实现全部业务功能：

- `question.md`
- `attachments/wireframes.md`
- `attachments/api-spec.md`
- `attachments/buggy-component.tsx`

本阶段目标：初始化一个可运行的前端工程，并搭建基础工程骨架。

要求如下：

1. 使用 Vite 创建 React + TypeScript 前端项目。
2. 包管理器使用 `npm`，不要使用 pnpm。
3. 前端项目建议创建在仓库根目录下，如果当前目录还不是前端项目，请在当前目录初始化，不要放到 mock-server 目录里。
4. 安装并配置这些基础依赖：
   - React 18+
   - TypeScript
   - Vite
   - Ant Design
   - @tanstack/react-query
   - Vitest
   - React Testing Library
5. 配置基础开发命令：
   - `npm install`
   - `npm run dev`
   - `npm run build`
   - `npm run test`
6. 配置 API base URL：
   - 默认使用 `http://localhost:3001`
   - 支持通过 `.env` 中的 `VITE_API_BASE_URL` 覆盖
7. 建立基础目录结构：

```txt
src/
  api/
    client.ts
    buildings.ts
    devices.ts
    alerts.ts
    workOrders.ts
    chat.ts
  types/
    domain.ts
    chat.ts
  hooks/
  components/
    layout/
    devices/
    work-orders/
    chat/
  pages/
  utils/
```

8. 在 `src/types/domain.ts` 中定义基础业务类型：
   - Building
   - Device
   - DeviceDetail
   - Alert
   - WorkOrder
   - DeviceStatus
   - DeviceType
   - WorkOrderStatus
   - Priority

9. 在 `src/types/chat.ts` 中定义聊天相关类型：
   - ChatRole
   - ChatMessage
   - ToolCall
   - ChatResponse

10. 在 `src/api/client.ts` 中封装一个通用 request 函数：
    - 自动拼接 `VITE_API_BASE_URL`
    - 支持 query params
    - 支持 GET / POST / PATCH
    - 对非 2xx 响应抛出包含 message 的错误
    - 尽量保持类型安全

11. 在各 API 文件中先封装接口函数，但本阶段不需要做完整页面：
    - `getBuildings`
    - `getDevices`
    - `getDeviceDetail`
    - `getAlerts`
    - `ackAlert`
    - `getWorkOrders`
    - `createWorkOrder`
    - `updateWorkOrderStatus`
    - `sendChat`

12. 创建一个最小可运行的 `App.tsx`：
    - 使用 `QueryClientProvider`
    - 使用 Ant Design 的基础布局
    - 页面显示标题“星汇智慧空间”
    - 显示一段提示：mock server 应运行在 `http://localhost:3001`
    - 可以有一个简单按钮用于测试请求 `/api/buildings`，成功后展示楼栋数量，失败时展示错误提示

13. 添加基础测试配置：
    - 使用 Vitest
    - 添加 Testing Library
    - 至少写一个简单测试，验证 App 能正常渲染“星汇智慧空间”

14. 添加或更新 README：
    - 说明如何启动 mock server：
      ```bash
      cd attachments/mock-server
      npm install
      npm start
      ```
    - 说明如何启动前端：
      ```bash
      npm install
      npm run dev
      ```
    - 说明如何构建和测试：
      ```bash
      npm run build
      npm run test
      ```

15. 不要修改 `attachments/mock-server` 里的 mock server 代码，除非发现无法运行。这个阶段只需要前端工程可以启动、构建通过、测试通过。

完成后请运行：

```bash
npm run build
npm run test
```

如果有报错，请修复到通过为止。

最后请总结：
- 创建/修改了哪些关键文件
- 如何启动 mock server 和前端
- build/test 是否通过