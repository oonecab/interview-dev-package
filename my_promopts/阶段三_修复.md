请在当前 React + TypeScript + Ant Design + React Query 项目中先修复阶段三 review 发现的问题，不要进入 AI 助手阶段四。

背景：
- 项目需求见 question.md、attachments/wireframes.md、attachments/api-spec.md、milestone/development-milestones.md。
- 当前已经完成阶段三：设备看板 + 工单管理。
- npm run test 和 npm run build 当前通过，请修复后继续保持通过。

需要修复：

1. 设备统计概览不要被状态/类型筛选污染
- 当前 src/components/devices/DeviceDashboard.tsx 用同一个已筛选 devices 同时驱动表格和 DeviceStats。
- 请改成：
  - 表格继续使用 buildingId + status + type 查询结果。
  - DeviceStats 使用当前 buildingId 下的全量设备统计，只受 buildingId 影响，不受 status/type 影响。
  - 页面上的“共 N 台设备”可以保留为当前表格结果数量，但统计卡片必须展示当前楼栋全量概览。
- 注意 React Query queryKey 要清晰区分全量统计查询和表格筛选查询。

2. 统一 devices queryKey 语义
- 当前 CreateWorkOrderModal.tsx 用 ['devices', 'all'] 查询全部设备，DeviceDashboard.tsx 用 ['devices', queryParams]。
- 请提取或约定一个统一、可读的 queryKey 风格，例如：
  - ['devices', 'list', params]
  - ['devices', 'detail', deviceId]
- 修改相关组件，确保 invalidate/refetch 行为不被破坏。
- 不要大规模引入新抽象，只做必要整理。

3. WorkOrderDetailDrawer 改用 Ant Design App context message
- 当前 src/components/work-orders/WorkOrderDetailDrawer.tsx 直接从 antd import message。
- 请改为使用 App.useApp()，和 CreateWorkOrderModal.tsx 保持一致。

4. 补充至少一个有意义的测试
- 最终题目要求至少覆盖 3 个组件或函数；当前只有 App 和 statusFlow。
- 请新增一个工单模块相关测试，优先覆盖以下任一场景：
  - getNextWorkOrderStatus 已有测试之外，测试 WorkOrderDetailDrawer 点击“推进到下一状态”会调用 PATCH 并 invalidate/refetch；
  - 或测试 CreateWorkOrderModal 表单必填校验/提交成功调用 POST；
  - 或测试 WorkOrderTable 能渲染状态、优先级、关联设备并响应行点击。
- 测试保持稳定，不依赖真实 mock server，使用 mock fetch 或 mock API 函数。

验收：
- npm run test 必须通过。
- npm run build 必须通过。
- 修复范围尽量小，不要实现阶段四 AI 聊天 UI。
- 最后请总结修改了哪些文件、为什么改，以及测试/构建结果。
