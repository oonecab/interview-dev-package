你现在在 `/Users/chunwang/Code/interview-dev-package` 工作。

阶段二设备看板已经基本完成，但 review 发现两个交付前需要修复的问题。请只修复以下问题，不要开始实现工单管理或 AI 助手。

## 问题 1：默认没有选中第一个楼栋

当前 `src/components/layout/AppShell.tsx` 中：

- `selectedBuildingId` 初始为 `null`
- `BuildingSidebar` 加载楼栋后没有自动选择第一个楼栋
- 首屏显示“请从左侧选择楼栋以查看设备”

这不符合阶段二要求：“默认选中第一个楼栋”。

请修复为：

1. 当 `getBuildings` 成功返回楼栋列表后，如果当前 `selectedBuildingId` 为空，则自动选择第一个楼栋。
2. 不要造成无限循环。
3. 不要重复引入额外状态管理库。
4. 优先在 `BuildingSidebar` 中用 `useEffect` 基于已加载的 `buildings` 调用 `onBuildingSelect(buildings[0].id)`。
5. 保持用户手动切换楼栋后不会被重置。
6. 如果楼栋列表为空，则保持当前空状态。

建议实现思路：

- 在 `BuildingSidebar.tsx` 中引入 `useEffect`
- 当 `buildings?.length` 存在且 `selectedBuildingId === null` 时调用 `onBuildingSelect(buildings[0].id)`
- 依赖数组包含 `buildings`、`selectedBuildingId`、`onBuildingSelect`

## 问题 2：补充 `.gitignore`

当前项目根目录存在：

- `node_modules/`
- `dist/`
- `*.tsbuildinfo`

最终交付不应该提交这些内容。请在项目根目录新增或更新 `.gitignore`，至少包含：

```txt
node_modules/
dist/
*.tsbuildinfo
.DS_Store
.env.local
.env.*.local
coverage/
```

不要删除现有文件，只需要补充 `.gitignore`。

## 测试要求

修复后请运行：

```bash
npm run build
npm run test
```

如果测试因为默认选中楼栋逻辑变化失败，请更新测试。

可以补充一个简单测试：

- mock `/api/buildings` 返回至少一个楼栋
- 验证页面最终出现该楼栋名称，或验证不再只停留在“请从左侧选择楼栋以查看设备”

## 完成后请总结

请说明：

- 修改了哪些文件
- 默认选中楼栋逻辑如何实现
- build/test 是否通过
- 是否还有后续阶段 TODO
