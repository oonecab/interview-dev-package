请基于当前项目继续开发 milestone/development-milestones.md 的阶段 9：体验打磨。先阅读：
- question.md
- attachments/wireframes.md
- milestone/development-milestones.md
- 当前 src/components/layout/AppShell.tsx
- 当前 src/components/devices/*
- 当前 src/components/alerts/*
- 当前 src/components/work-orders/*
- 当前 src/components/chat/*
- 当前 src/utils/format.ts、labels.ts、error.ts
- 当前测试文件，避免破坏已有行为

当前状态：
- 阶段 8 已完成，npm run test 通过：10 个测试文件，54 个测试。
- npm run build 通过。
- 现在不要新增大功能，不要写最终交付文档。目标是让项目看起来像成熟后台管理工具，而不是功能堆叠。

阶段九目标：
体验打磨，包括：
- 统一中文文案
- 统一时间格式
- 统一状态标签颜色
- 优化表格 loading / empty / error 状态
- 优化 AI 侧栏滚动体验
- 优化工具调用卡片
- 优化表单交互
- 检查 1280px 宽度下布局无横向溢出
- 保持当前功能和测试全部通过

具体要求：

1. 全局布局打磨
- 检查 AppShell 的三栏布局：
  - 顶部导航高度稳定。
  - 左侧楼栋筛选栏宽度稳定。
  - 主内容区在 AI 侧栏展开时不横向溢出。
  - 右侧 AI 侧栏 360px 保持稳定。
- 最小支持 1280px 宽，不需要移动端。
- 避免大面积 hero、装饰性背景、营销风布局，保持后台管理工具风格。
- 避免卡片套卡片；如果当前确实有必要卡片，保持紧凑。

2. 设备看板体验
- 设备统计区：
  - 保持各类型数量和各状态数量清晰。
  - 不要因为筛选导致统计误读，继续使用当前楼栋全量设备统计。
  - 如能小幅提升扫描性，可以调整间距/标题/颜色，但不要大改结构。
- 设备表格：
  - loading、空状态、错误状态文案统一。
  - 表格列宽、时间列、状态标签不要挤压。
  - 点击行打开详情仍可用。
- 设备详情抽屉：
  - 标题、状态、类型、楼层、时间格式保持统一。
  - 最近告警列表样式与告警面板一致或接近。
  - 创建工单按钮位置清晰。

3. 告警面板体验
- DeviceAlertPanel：
  - 过滤器文案统一：全部、严重、警告、信息、未确认。
  - 自动刷新开关文案清晰，不挤压。
  - 列表项信息密度合理：设备名、消息、时间、级别、确认按钮。
  - 已确认状态视觉明显但不过度抢眼。
  - 加载、错误、空状态文案统一。
- 不要改变阶段六修复逻辑：
  - buildingId 变化必须重新拉取；
  - 自动刷新必须可关闭；
  - 确认告警必须不可变更新。

4. 工单管理体验
- WorkOrderPage：
  - 顶部标题、数量、状态筛选、创建按钮布局更整齐。
  - 表格空状态区分“暂无工单”和“当前筛选条件下暂无工单”。
- CreateWorkOrderModal：
  - 表单交互打磨：
    - 提交中按钮 loading。
    - 设备下拉加载中/无设备时文案清晰。
    - 表单字段 placeholder 统一。
  - 提交失败不关闭弹窗，保持当前行为。
- WorkOrderDetailDrawer：
  - 状态流转 Steps 和“推进到下一状态”按钮视觉清晰。
  - 已完成状态提示清楚。
  - PATCH 失败不关闭抽屉，保持当前行为。

5. AI 助手体验
- ChatSidebar：
  - 消息区滚动体验打磨：
    - 新消息和工具卡片出现时自动滚到底部。
    - 底部输入区固定。
    - 长消息不撑破侧栏。
  - 欢迎消息文案保持简洁。
  - 用户消息、assistant 消息、工具卡片视觉层级清楚。
  - 发送中状态清楚，输入禁用。
- 工具调用卡片：
  - 工具名、参数、状态、结果摘要排列更易读。
  - query_devices/query_alerts/create_work_order 的显示名统一。
  - 失败状态醒目但不过度。
  - 参数很长时换行或截断，不能撑破 360px 侧栏。
- 不要改变 Tool Calling 协议逻辑：
  - buildApiMessages 仍必须正确构建 assistant tool_calls 和 tool 消息。
  - create_work_order 成功后仍刷新工单列表。
  - 失败不能假成功。

6. 文案和格式统一
- 检查中文文案：
  - “暂无数据 / 当前筛选条件下暂无...” 风格统一。
  - “加载失败 / 重试 / 创建成功 / 确认成功”风格统一。
  - 不要出现中英文混杂的不必要文案。
- 检查时间格式：
  - 统一使用 src/utils/format.ts 的 formatDateTime / formatFloor。
  - 不要在组件内临时 new Date(...).toLocaleString()。
- 检查状态颜色：
  - 继续使用 src/utils/labels.ts 中的颜色映射。
  - 不要在多个组件里随意新增另一套颜色语义。
  - 如果需要优化颜色，集中改 labels 或局部解释清楚。

7. CSS / 样式范围
- 当前大多是 inline style，可以继续保持，不要引入新 CSS 框架。
- 如果新增 CSS 文件，范围要小、命名清楚。
- 不要引入新 UI 库。
- 不要做大规模主题重写。
- 不要用夸张渐变、装饰性图形、hero、营销页风格。
- 保持 Ant Design 管理后台风格。

8. 测试与回归
- 打磨样式时不要随意改测试可见文案。如果确实改文案，请同步更新测试。
- 如新增小交互，例如设备 Select 无数据文案，可以补一条轻量测试。
- 阶段九结束必须运行：
  - npm run build
  - npm run test

9. 非阻塞测试小缺口，可顺手补但不是必须
如果时间允许，请顺手补一条 DeviceAlertPanel 自动刷新关闭/卸载不再刷新测试：
- vi.useFakeTimers
- 初次请求后 advanceTimersByTime(5000) 验证会刷新
- 关闭自动刷新后再 advanceTimersByTime(5000) 验证不会新增请求
- unmount 后再 advanceTimersByTime(5000) 验证不会新增请求
如果这条测试实现复杂或不稳定，可以不要硬做，不要牺牲测试稳定性。

10. 不要做阶段十
- 不要写最终 README/设计说明/Bug 修复说明/AI 工具使用记录。
- 不要初始化 git。
- 不要修改 mock server。
- 不要进入最终交付文档阶段。

验收：
- npm run build 必须通过。
- npm run test 必须通过。
- 最后总结：
  - 打磨了哪些页面/组件；
  - 是否调整了文案、布局、时间格式、状态标签；
  - 是否补了测试；
  - npm run build / npm run test 结果。
