请基于当前项目继续开发 milestone/development-milestones.md 的阶段 8：测试完善。先阅读：
- question.md
- milestone/development-milestones.md
- 当前 src/components/alerts/DeviceAlertPanel.tsx 和测试
- 当前 src/components/chat/toolExecutor.ts、ChatSidebar.tsx、chatProtocol.ts 和测试
- 当前 src/components/work-orders/CreateWorkOrderModal.tsx、WorkOrderDetailDrawer.tsx、WorkOrderTable.tsx
- 当前 src/components/devices/DeviceStats.tsx、DeviceDashboard.tsx、DeviceTable.tsx
- src/setupTests.ts 和 vitest.config.ts

当前状态：
- 阶段 7 已完成，npm run test 通过：6 个测试文件，38 个测试。
- npm run build 通过。
- 现在目标不是新增业务功能，而是让测试覆盖更有价值、更稳定、更能支撑最终交付。

阶段八目标：
完善测试，覆盖至少 3 个组件或函数，并优先覆盖真正有价值的行为：
- statusFlow
- Tool Calling 工具执行器
- DeviceAlertPanel
- 创单表单校验/失败
- 设备统计聚合逻辑
同时清理 jsdom 下 Ant Design 的无意义 warning，保证测试稳定、可重复。

具体要求：

1. 给 toolExecutor 增加独立单元测试
新增：
- src/components/chat/toolExecutor.test.ts

mock 业务 API，不依赖真实 fetch 或 mock server。可以使用 vi.mock mock：
- ../../api/devices 的 getDevices
- ../../api/alerts 的 getAlerts
- ../../api/workOrders 的 createWorkOrder

至少覆盖：
- query_devices：参数 buildingId/status/type 被正确传给 getDevices，返回内容 JSON.stringify，summary 为“返回 N 台设备”。
- query_alerts：参数 buildingId/level 被正确传给 getAlerts，summary 为“返回 N 条告警”。
- create_work_order：title/description/deviceId/priority 正确传给 createWorkOrder，summary 包含工单编号。
- invalid JSON arguments：返回 success=false，summary 为“工具参数解析失败”。
- unknown tool：返回 success=false，summary 包含“未知工具”。
- API reject：返回 success=false，summary 是对应失败文案。
注意：这些是核心考点测试，尽量保持简单、直接、稳定。

2. 给 DeviceStats 增加聚合测试
新增：
- src/components/devices/DeviceStats.test.tsx

覆盖：
- 不同 type 的设备数量统计正确。
- 不同 status 的数量统计正确。
- 空设备数组时各项显示 0。
这能证明统计逻辑可信，避免后续修改时误把筛选后的列表当全量统计。

3. 强化 DeviceAlertPanel 自动刷新测试
当前 DeviceAlertPanel.test.tsx 中“auto-refresh can be toggled off”没有严格断言。
请改成使用 vi.useFakeTimers，并确保测试稳定：
- 初次渲染会请求一次 alerts。
- 自动刷新开启时，advanceTimersByTime(5000) 后会再次请求。
- 关闭自动刷新后，再 advanceTimersByTime(5000) 不应继续触发新的请求。
- 组件卸载后，再 advanceTimersByTime(5000) 不应触发请求。
注意 React Query refetchInterval 和 Testing Library 的 act 配合，必要时用 act 包裹 advanceTimersByTime。
测试结束必须 vi.useRealTimers，避免污染其他测试。

4. 增加 DeviceAlertPanel 筛选测试
如果还没有充分覆盖，请补：
- 选择 “警告” 只显示 warning。
- 选择 “信息” 只显示 info。
- 选择 “未确认” 只显示 acknowledged=false。
当前已有“严重无结果”测试，但还需要至少一个正向筛选测试。

5. 给 CreateWorkOrderModal 增加表单/失败测试
新增：
- src/components/work-orders/CreateWorkOrderModal.test.tsx

mock fetch 或 mock API，包裹 QueryClientProvider + Ant Design App。
至少覆盖：
- 点击提交但必填项为空，显示标题/描述/关联设备必填校验文案，不调用 POST。
- 填完整表单后 POST /api/work-orders 成功，会调用 onClose。
- POST 失败时不调用 onClose，弹窗仍保留。
可选优化：
- 如果设备列表加载失败或为空，Select 显示“设备加载失败”或“暂无可选设备”。如果当前代码没有这个 UI，可以做小范围补充。

6. 给 WorkOrderDetailDrawer 状态推进失败增加测试
可以新增：
- src/components/work-orders/WorkOrderDetailDrawer.test.tsx

覆盖：
- pending 工单点击“推进到已派单”会 PATCH /api/work-orders/:id。
- PATCH 成功时调用 onClose。
- PATCH 失败时不调用 onClose，抽屉仍显示。
如果测试 AntD message 不好断言，可以只断言 onClose 是否调用和按钮恢复即可。

7. 检查并清理测试 warning
- 运行 npm run test，看控制台是否有大量 jsdom / Ant Design warning。
- 如果有无意义 warning，优先在 src/setupTests.ts 做最小 polyfill，而不是在每个测试里散落 mock。
- 不要为了消 warning 掩盖真实错误。
- 当前已有 matchMedia、getComputedStyle、scrollIntoView polyfill，必要时补 ResizeObserver 等。

8. 保持范围
- 不要进入阶段九体验打磨。
- 不要写最终设计说明、Bug 修复说明、AI 工具使用记录。
- 不要修改 attachments/mock-server。
- 不要大规模重构业务组件。
- 可以为了可测试性做小范围改动，例如导出纯函数、增加 Select 的 notFoundContent/status，但必须保持 UI 行为一致。

9. 验收
完成后必须运行：
- npm run test

如果因小范围测试驱动改了业务代码，也请运行：
- npm run build

最终总结：
- 新增/修改了哪些测试文件；
- 覆盖了哪些高价值行为；
- 是否清理了 warning；
- npm run test 结果；
- 如运行了 npm run build，也报告结果。
